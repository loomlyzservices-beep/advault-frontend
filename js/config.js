// Point this at your deployed backend's URL, e.g. https://advault-backend.up.railway.app
// Leave as '' only if you are serving the frontend from the SAME Railway service as the backend.
export const API_BASE = window.ADVAULT_API_BASE || 'http://localhost:3000'
